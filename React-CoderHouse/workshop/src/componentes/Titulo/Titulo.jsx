import React from 'react'

const Titulo = ({title, subtitulo}) => {   
        
    return(
        <div>
            <h1>{title}</h1>
            <h3>{subtitulo}</h3>
        </div>
    )
};

export default Titulo